login:
  type: object
  properties:
    username:
      type: string
      example: username
      description: ユーザー名
    password:
      type: string
      format: password
      example: password
      description: パスワード
  required:
    - username
    - password