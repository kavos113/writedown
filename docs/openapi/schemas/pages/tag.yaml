tag:
  type: object
  properties:
    id:
      type: integer
      example: 1
      description: タグID
    name:
      type: string
      example: tag
      description: タグ名
  required:
    - id
    - name

tags:
  type: array
  items:
    $ref: '#/tag'