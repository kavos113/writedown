patchPage:
  type: object
  properties:
    id:
      type: integer
      example: 1
    path:
      type: string
      example: /path/to/page
    title:
      type: string
      example: title
    body:
      type: string
      example: wikiページの内容
    creator:
      type: string
      example: owner